# 济宁菜馆扫码点餐小程序 - 产品需求文档 (PRD)

> **版本**: v1.1  
> **日期**: 2026-08-06  
> **产品类型**: 微信小程序  
> **目标用户**: 良子饭店顾客 + 饭店管理运营

---

## 一、项目概述

### 1.1 产品定位

一款专为**良子饭店**（济宁菜馆）定制的微信扫码点餐小程序。顾客扫描桌上二维码即可浏览菜单、下单支付；饭店工作人员通过同一小程序登录管理端，完成订单处理、菜品管理、经营配置等全部操作，无需电脑。

### 1.2 核心场景

| 场景       | 流程                                                  |
| -------- | --------------------------------------------------- |
| **堂食点餐** | 顾客到店 → 落座 → 扫桌上二维码 → 浏览菜单 → 加购下单 → 微信支付 → 厨房备菜 → 上菜 |
| **外卖配送** | 顾客在家 → 打开小程序 → 选择外卖配送 → 选择菜品 → 填写地址 → 微信支付 → 自有员工配送 |

### 1.3 当前规模

- **桌台数量**：10 张（编号 1 ~ 10 号）
- **配送方式**：自有员工配送
- **支付模式**：先付后吃（堂食与外卖均需在线支付后厨房才出单）
- **厨房设备**：无需小票打印，服务员手机查看订单后口头传达

---

## 二、用户角色

| 角色 | 身份 | 权限 |
|------|------|------|
| **顾客** | 微信用户（无需注册） | 扫码进入 → 浏览菜单 → 下单支付 → 查看订单 |
| **管理端** | 员工/店长（后台添加账号） | 订单处理 + 菜品管理（含新增/改价/上下架）+ 桌台管理 + 员工管理 + 营业数据 |

> 管理端不再区分"服务员"和"管理员"——所有功能集中在同一入口，靠账号体系控制谁能登录。

---

## 三、功能需求

### 3.1 顾客端功能

#### 3.1.1 扫码进入 → 桌台绑定

```
扫码（含桌号参数的二维码）→ 授权获取微信头像昵称 → 自动绑定桌号 → 进入菜单页
```

- 每个桌子贴唯一二维码（URL 带 table_no 参数，如 `?table_no=3`）
- 顾客扫码后自动识别桌号，顶部显示桌号信息
- 同一张桌子多人扫码：后扫的人看到当前桌已点的菜品（购物车合并或独立？→ 独立购物车，每人扫自己的，各付各的）
- 离店后会话自动过期（30分钟无操作自动释放，可配置）

#### 3.1.2 菜单浏览与点餐

```
菜单页（双栏布局）→ 左侧分类 / 右侧菜品列表 → 点击菜品 → 选择规格 → 加入购物车
```

**菜品结构**：

- 分类 → 菜品 → 规格（如：热菜 → 糖醋里脊 → 大份/小份、辣度选择）
- 规格层级：
  - 单品：直接加购，默认数量 1
  - 多规格：弹出规格选择窗（如大小份、口味偏好）

**购物车**：

- 底部常驻购物车栏，显示总数量 + 总金额
- 点击展开购物车详情弹窗
- 支持增减数量、删除单品、清空购物车
- 购物车与桌号关联，本地持久化存储

#### 3.1.3 堂食 vs 外卖模式切换

- 顾客扫码进入默认为对应桌号的**堂食模式**
- 在菜单页顶部可切换为**外卖模式**
- 外卖模式：
  - 无需绑定桌号
  - 需填写收货地址（含姓名 + 电话 + 详细地址）
  - 显示起送价门槛（外卖免配送费）

#### 3.1.4 下单与支付

```
购物车 → 确认订单 → 选择堂食/外卖 → 确认桌号/地址 → 微信支付 → 下单成功 → 订单详情页
```

- **堂食订单**：
  - 确认桌号
  - 直接支付
- **外卖订单**：
  - 确认或新增收货地址
  - 外卖免配送费
  - 起送价校验，不满足则提示
- **支付**：调用微信支付 JSAPI，仅支持微信支付
- **支付成功**：跳转订单详情页，服务员端同步收到新订单通知

#### 3.1.5 订单查看

```
个人中心 → 我的订单 → 订单列表（按状态分类）→ 订单详情
```

- 订单列表分 Tab：全部 / 堂食 / 外卖
- 订单状态流：待支付 → 已支付（备菜中）→ 已上菜/配送中 → 已完成 → 已取消
- 支持取消订单（仅待支付状态可取消）

#### 3.1.6 个人中心

- 微信头像 + 昵称展示
- 我的订单入口
- 收货地址管理（外卖用）

---

### 3.2 管理端功能（同一个小程序内）

管理端登录后展示 4 个底部 Tab：**堂食** / **外卖** / **菜品** / **经营**。所有饭店运营操作全部在手机上完成。

#### 3.2.1 管理端登录

```
顾客端首页 → 管理入口（底部小字链接）→ 输入账号密码 → 进入管理端视图
```

- 管理账号由已有管理员在「经营→员工管理」中创建
- 登录后 token 有效期 2 小时

#### 3.2.2 Tab 1：堂食订单

```
按桌号分组卡片 → 查看每桌已点菜品 → 逐菜标记"已上菜" → 整桌完成
```

- 10 张桌子按序号排列，有订单的桌号高亮
- 每张卡片：桌号 + 人数 + 菜品明细 + 总金额 + 下单时间
- 操作：**逐菜勾选已上** / **一键全上** / **标记完成**

#### 3.2.3 Tab 2：外卖订单

```
备菜中列表 → 开始配送 → 标记配送中 → 标记已送达
```

- 新订单声音 + 列表高亮提醒
- 显示菜品、地址、电话、顾客备注

#### 3.2.4 Tab 3：菜品管理

##### 菜品列表

```
┌──────────────────────────────────────┐
│   [全部] [凉菜] [热菜] [汤品] [主食]    │  ← 分类Tab
├──────────────────────────────────────┤
│ ┌────────────────────────────────┐   │
│ │ 🍗 糖醋里脊                    │   │
│ │    点击改价→ ¥38  ←           │   │  ← 点价格弹出数字键盘
│ │         大份·小份              │   │
│ │                     [上架] ▼   │   │  ← 点 ▼ 弹出三选一
│ └────────────────────────────────┘   │
│ ┌────────────────────────────────┐   │
│ │ 🔴售罄  🐟 微山湖鲤鱼          │   │  ← 红色角标
│ │    ¥68 | 明日0点自动恢复        │   │
│ │                     [售罄] ▼   │   │
│ └────────────────────────────────┘   │
│ ┌────────────────────────────────┐   │
│ │ 🥬 孔府豆腐  (灰色半透明)       │   │  ← 下架菜品视觉降权
│ │    ¥22                         │   │
│ │                     [下架] ▼   │   │
│ └────────────────────────────────┘   │
│                                      │
│         [+ 新增菜品]                  │  ← 进入新增表单
└──────────────────────────────────────┘
```

##### 菜品三态

| 状态 | 顾客端表现 | 切换方式 |
|------|-----------|---------|
| **上架** | 正常显示，可下单 | 点 ▼ → 选"上架" |
| **售罄** | 显示但标记"已售罄"，不可加购 | 点 ▼ → 选"售罄" |
| **下架** | 完全隐藏 | 点 ▼ → 选"下架" |

- 售罄菜品**每日 00:00 自动恢复上架**（后端定时任务）
- 切换即时生效，无需确认

##### 快速改价

- 点价格数字 → 弹出数字键盘 → 输入新价格 → 回车保存
- 仅改单价，不影响其他字段

##### 新增菜品

- 点底部「+ 新增菜品」→ 进入表单页：
  - 上传图片（拍照或相册选取）
  - 输入名称、选择分类
  - 输入价格
  - 添加规格组（如"大小份"）+ 各规格价格偏移
  - 保存后默认上架

##### 编辑/删除菜品

- 列表项左滑露出「编辑」「删除」按钮
- 编辑进入同新增表单（预填已有数据）
- 删除弹出二次确认

#### 3.2.5 Tab 4：经营

功能列表页，每项进入子页面：

| 功能 | 说明 |
|------|------|
| **桌台管理** | 查看 10 张桌二维码 → 长按保存到相册 → 打印张贴 |
| **员工管理** | 添加/删除管理账号、重置密码 |
| **配送设置** | 起送价（免配送费） |
| **营业数据** | 今日订单数+营业额、近7天趋势、热销菜品排行 |

---

## 四、非功能需求

### 4.1 性能

| 指标     | 要求         |
| ------ | ---------- |
| 页面首屏加载 | < 2 秒      |
| 接口响应时间 | < 500ms    |
| 支付回调处理 | < 1 秒      |
| 并发支持   | 10桌同时点餐无卡顿 |

### 4.2 安全

- 桌号二维码参数加密，防止篡改桌号（JWT token 方式）
- 微信支付回调签名验证
- 员工端账号密码登录，token 过期机制（2小时）
- 接口防重放攻击（订单幂等性）

### 4.3 兼容性

- 微信小程序基础库 ≥ 2.10.0
- iOS 12+ / Android 7+
- 微信版本 7.0+

### 4.4 数据存储

- 订单数据永久保留
- 顾客购物车缓存30分钟自动清除

---

## 五、技术架构

### 5.1 技术选型

| 层级       | 技术                             | 说明                                |
| -------- | ------------------------------ | --------------------------------- |
| **前端**   | uni-app + Vue 2 + uView UI 2.x | 复用 sky-shaokao_1 插件框架，一套代码编译微信小程序 |
| **后端**   | Node.js + Koa2                 | 轻量高性能，JS全栈统一语言                    |
| **数据库**  | MySQL 8.0                      | 订单/菜品/用户结构化数据                     |
| **缓存**   | Redis 6.x                      | 扫码会话缓存、微信 token 缓存、订单状态临时锁        |
| **对象存储** | 腾讯云 COS / 阿里云 OSS              | 菜品图片上传存储                          |
| **部署**   | 腾讯云轻量应用服务器 / 阿里云 ECS           | 单机部署，仅需一台服务器                      |

### 5.2 系统架构图

```
┌─────────────────────────────────────────────┐
│                 微信小程序                     │
│  ┌──────────────┐  ┌──────────────────────┐ │
│  │   顾客端       │  │   管理端（4 Tab）      │ │
│  └──────┬───────┘  └──────────┬───────────┘ │
└─────────┼─────────────────────┼──────────────┘
          │                     │
          ▼                     ▼
┌─────────────────────────────────────────────┐
│              Nginx (反向代理)                  │
└─────────────────────┬───────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────┐
│              Koa2 API 服务                    │
│   /api/customer/*  顾客端接口                 │
│   /api/manage/*    管理端接口                 │
└──────────┬──────────────┬───────────────────┘
           │              │
           ▼              ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│    MySQL     │  │    Redis     │  │   COS/OSS    │
└──────────────┘  └──────────────┘  └──────────────┘
```

### 5.3 接口规范

- RESTful API
- 统一返回格式：`{ code: 1, msg: "success", data: {} }`
- code=1 成功，code=0 业务错误，code=-1 鉴权失败
- 请求头：`Authorization: Bearer <token>`（顾客端用微信openid换token）

---

## 六、数据库设计（核心表）

### 6.1 ER 概要

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│ category │────→│  product │────→│ product  │
│  分类     │ 1:N │  菜品     │ 1:N │  _spec   │
└──────────┘     └──────────┘     │  规格     │
                                  └──────────┘
                                       │
┌──────────┐     ┌──────────┐     ┌────┴─────┐
│  table   │────→│  order   │←────│ order    │
│  桌台     │ 1:N │  订单     │ 1:N │  _item   │
└──────────┘     └──────────┘     │  订单明细 │
                                  └──────────┘
                                       │
┌──────────┐     ┌──────────┐          │
│  staff   │────→│  order   │ ←────────┘
│  员工     │ 1:N │  _flow   │
└──────────┘     │  操作流水 │
                 └──────────┘
```

### 6.2 核心表清单

| 表名              | 说明   | 关键字段                                                                                                                                      |
| --------------- | ---- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `users`         | 顾客表  | id, openid, nickname, avatar, phone, created_at                                                                                           |
| `addresses`     | 收货地址 | id, user_id, name, phone, province, city, district, detail, is_default                                                                    |
| `categories`    | 菜品分类 | id, name, sort, icon, status                                                                                                              |
| `products`      | 菜品   | id, category_id, name, image, description, price, status(1上架/2售罄/0下架), sort, is_hot |
| `product_specs` | 菜品规格 | id, product_id, name, price_delta（+/-价格偏移）                                                                                                |
| `tables`        | 桌台   | id, table_no(1-10), qrcode_url, status(free/occupied)                                                                                     |
| `orders`        | 订单   | id, order_no, user_id, table_id, type(dine_in/delivery), total_amount, delivery_fee, status, address_id, people_count, remark, created_at |
| `order_items`   | 订单明细 | id, order_id, product_id, product_name, spec_name, price, quantity, status(待上菜/已上菜)                                                       |
| `order_flow`    | 订单流水 | id, order_id, staff_id, action, remark, created_at                                                                                        |
| `staff`         | 员工   | id, username, password_hash, name, role(waiter/admin), status                                                                             |
| `config`        | 系统配置 | id, key, value（起送价等）                                                                                                                  |

---

## 七、页面路由设计

基于 sky-shaokao_1 插件现有结构改造：

### 7.1 顾客端页面

| 路由                       | 页面    | 说明                  |
| ------------------------ | ----- | ------------------- |
| `/`                      | 首页    | 扫码后落地页，自动识别桌号，进入菜单  |
| `/pages/menu/menu`       | 菜单/点餐 | 双栏菜单 + 购物车 + 堂食外卖切换 |
| `/pages/order/order`     | 我的订单  | 订单列表 + 订单详情         |
| `/pages/order/submit`    | 确认订单  | 下单确认 + 支付           |
| `/pages/address/address` | 收货地址  | 地址增删改查              |
| `/pages/my/my`           | 个人中心  | 用户信息 + 功能入口         |

### 7.2 管理端页面（4 Tab）

| Tab | 路由 | 说明 |
|-----|------|------|
| 堂食 | `/pages/manage/dine-in` | 按桌号订单管理 |
| 外卖 | `/pages/manage/delivery` | 配送订单管理 |
| 菜品 | `/pages/manage/menu` | 菜品列表 + 三态切换 + 改价 |
| 经营 | `/pages/manage/settings` | 功能列表（桌台/员工/配送/数据） |

##### 菜品管理子页面

| 路由 | 说明 |
|------|------|
| `/pages/manage/menu/edit` | 新增/编辑菜品（含规格设置） |

##### 经营子页面

| 路由 | 说明 |
|------|------|
| `/pages/manage/settings/tables` | 桌台二维码管理 |
| `/pages/manage/settings/staff` | 员工账号管理 |
| `/pages/manage/settings/delivery-config` | 起送价设置 |
| `/pages/manage/settings/analytics` | 营业数据统计 |

管理端入口：`/pages/manage/login`（账号密码登录）

---

## 八、扫码流程详解

### 8.1 二维码生成

```
后台生成每桌二维码 → 链接格式：
https://xxx.com/scan?t=<加密token>

其中 token 由后端 JWT 签发：
{ table_no: 3, exp: 长期有效 }
```

### 8.2 顾客扫码流程

```
1. 顾客扫桌上二维码
2. 小程序启动，解析 URL 参数中的 token
3. 调用后端 /api/table/decode 解密获取桌号
4. 检查该顾客是否已有该桌的活跃会话
   - 有 → 恢复之前会话（购物车数据）
   - 无 → 创建新会话
5. 授权获取微信头像昵称
6. 进入菜单点餐页，顶部显示桌号 "3号桌"
```

### 8.3 会话生命周期

```
创建 → 活跃（30分钟倒计时）→ 每次操作刷新 TTL → 超时自动销毁 → 购物车清除
     → 顾客下单支付后 → 会话标记为"已下单"，不再清理购物车
```

---

## 九、订单状态机

### 9.1 堂食订单

```
待支付 ──支付成功──→ 已支付(备菜中) ──服务员逐个上菜──→ 部分上菜 ──全部上齐──→ 已上齐 ──顾客用餐完毕──→ 已完成
  │                                                                                              
  └──取消──→ 已取消（仅待支付状态可取消）
```

### 9.2 外卖订单

```
待支付 ──支付成功──→ 备菜中 ──开始配送──→ 配送中 ──送达──→ 已完成
  │
  └──取消──→ 已取消
```

---

## 十、关键业务规则

| 规则          | 说明                               |
| ----------- | -------------------------------- |
| **加购无重复下单** | 同一桌号同一菜品同规格，第二次加购是增加数量，不是创建新条目   |
| **支付幂等**    | 同一订单号重复支付回调只处理一次（Redis 分布式锁）     |
| **桌号不可篡改**  | 桌号由 JWT token 解析，前端不可传入，防止顾客修改桌号 |
| **员工接单通知**  | 新订单支付成功后，员工端实时轮询（5秒间隔）获取新订单      |
| **外卖起送价**   | 菜品小计 < 起送价时，"去支付"按钮置灰，提示还差多少     |
| **配送费**     | 外卖免配送费，不向顾客收取配送费用               |

---

## 十一、开发计划

### 第一阶段：核心点餐流程（MVP）

**后端**：

- 数据库建表（全部表）
- 顾客端 API：桌台解码、菜品分类+列表、创建订单、微信支付
- 管理端 API：登录、堂食+外卖订单管理

**前端**：

- 基于 sky-shaokao_1 改造顾客端菜单页和购物车
- 接入微信支付
- 管理端登录 + 堂食订单 + 外卖订单页面

### 第二阶段：菜品管理 + 经营设置

- 管理端菜品管理页（列表 + 三态切换 + 快速改价 + 新增/编辑/删除）
- 管理端经营页（桌台二维码、员工管理、配送设置、营业数据）
- 售罄自动恢复定时任务

### 第三阶段：优化 + 上线

- 性能优化 + 异常处理完善
- 小程序审核提交

---

## 十二、已确认事项

- [x] 顾客是否需要"呼叫服务员"功能？ → **不需要**
- [x] 是否需要菜品评价/评分功能？ → **不需要**
- [x] 是否需要优惠券/满减活动？ → **不需要（当前版本）**
- [x] 管理后台是否部署在同一台服务器？ → **不设独立 Web 后台，全部管理功能在小程序内完成**
- [x] 小程序名称是否已确定？ → **良子饭店**
- [x] 菜品状态切换？ → **三态：上架 / 售罄（每日0点自动恢复）/ 下架**
- [x] 菜品管理是否需要快速改价？ → **需要，小程序管理端可以直接改**
- [x] 菜品管理是否需要新增/编辑/删除？ → **需要，全部在小程序管理端完成**

---

> **下一步**：PRD 确认后，我将开始搭建后端项目结构 + 改造前端代码。
